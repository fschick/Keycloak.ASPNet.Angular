export const environment = {
  apiBasePath: `${location.protocol}//${location.hostname}:5000/api`,
  authority: `${location.protocol}//${location.hostname}:8443/realms/asp-net-keycloak`,
  clientId: 'api',
};
